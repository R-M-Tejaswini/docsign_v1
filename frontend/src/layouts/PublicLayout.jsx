/**
 * ✅ PLACEHOLDER: Public layout for public signing pages
 * Removes navigation, keeps minimal UI
 */

export const PublicLayout = ({ children }) => {
  return <div className="min-h-screen bg-gray-50">{children}</div>
}